# NOTE: Kept for workflow_dispatch only. build-tests.yml handles PR/push triggers.
name: Build Tests

on:
  workflow_dispatch:

jobs:
  build_tests:
    uses: OpenVoiceOS/gh-automations/.github/workflows/build-tests.yml@fa39846b074ac7a856c8af5f8c0dec12b1fff1aa
    secrets: inherit
    with:
      package_name: 'json_database'
      version_file: 'json_database/version.py'
      python_versions: '["3.10", "3.11", "3.12", "3.13", "3.14"]'
      test_path: 'test/'
